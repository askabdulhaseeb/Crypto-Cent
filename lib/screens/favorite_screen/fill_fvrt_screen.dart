import 'package:flutter/material.dart';

class FillFvrtScreen extends StatelessWidget {
  const FillFvrtScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FillFvrtScreen'),
      ),
    );
  }
}
