import 'package:flutter/material.dart';

class SendBitcoinScreen extends StatelessWidget {
  const SendBitcoinScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SendBitcoinScreen'),
      ),
    );
  }
}
